apiVersion: v1
kind: Secret
metadata:
  name: dragon-secrets
type: Opaque
data:
  # Base64-encoded values; automation will render these from .env or Secrets Manager
  GEMINI_API_KEY: "${GEMINI_API_KEY_BASE64}"
  GROQ_API_KEY: "${GROQ_API_KEY_BASE64}"
  MONGO_URI: "${MONGO_URI_BASE64}"
